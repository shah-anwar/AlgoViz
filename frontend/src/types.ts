export interface Item {
    id: number;             // unique identifier
    name: string;           // name of the data structure (e.g., "Stack", "Queue")
    description: string;    // description of the data structure
    value: number;          // value or size of the data structure, if applicable
  }